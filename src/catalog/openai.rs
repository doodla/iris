//! Built-in catalog entries for the openai models. Filled in by the provider adapter task
//! from the verified research (see C-06); empty until then.

use super::types::ModelSpec;

pub static MODELS: &[ModelSpec] = &[];
