# Live verification files

This directory holds the offline test of `scripts/live-verify.sh`: `mock-run.sh`, and
`mock_providers.py`, the local mock of both provider APIs that it runs against.

For how to run the paid live verification, what it costs, and the rules that keep it from spending
more than you intend, see [Live testing](../../docs/contributing/live-testing.md).
