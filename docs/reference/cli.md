# CLI reference

This page shows the help of every Iris command, as `iris COMMAND --help` prints it. It's
generated from the same definitions as the help itself, so it matches the binary built from
the same commit. For the JSON that each command prints with `--json`, see the
[JSON output reference](json-output.md).

<!-- Generated by tests/cli_reference.rs. To update it, run: IRIS_UPDATE_DOCS=1 cargo test --test cli_reference -->

| Command | Description |
|---|---|
| [`iris`](#iris) | Generate and edit images and generate videos with OpenAI and Google Gemini/Veo |
| [`iris image`](#iris-image) | Generate and edit images (synchronous; no job record) |
| [`iris image generate`](#iris-image-generate) | Generate images from a prompt |
| [`iris image edit`](#iris-image-edit) | Edit images, or compose a new image from reference images |
| [`iris video`](#iris-video) | Generate videos (provider-native asynchronous jobs) |
| [`iris video generate`](#iris-video-generate) | Generate a video (waits and saves by default; --detach to submit and return) |
| [`iris jobs`](#iris-jobs) | List, inspect, wait for, download, and delete local video jobs |
| [`iris jobs list`](#iris-jobs-list) | List local job records (newest first) |
| [`iris jobs status`](#iris-jobs-status) | Show one job, refreshing its remote status once |
| [`iris jobs wait`](#iris-jobs-wait) | Wait for a job to finish, then download its outputs |
| [`iris jobs download`](#iris-jobs-download) | Download the outputs of a finished job (never resubmits) |
| [`iris jobs delete`](#iris-jobs-delete) | Delete LOCAL job records (no remote cancellation or deletion) |
| [`iris models`](#iris-models) | List models and inspect their capabilities |
| [`iris models list`](#iris-models-list) | List known models |
| [`iris models show`](#iris-models-show) | Show one model's capabilities, options, and prices |
| [`iris providers`](#iris-providers) | List providers, credential variables, and whether they are set |
| [`iris providers list`](#iris-providers-list) | List providers, credential variables, presence, and base URLs |
| [`iris config`](#iris-config) | Show effective configuration and file locations |
| [`iris config show`](#iris-config-show) | Show effective settings and the source of each value |
| [`iris config path`](#iris-config-path) | Show the config file, state directory, and jobs directory paths |
| [`iris doctor`](#iris-doctor) | Check credentials, configuration, and directories |
| [`iris schema`](#iris-schema) | Print the published JSON output schema |
| [`iris completions`](#iris-completions) | Print a shell completion script |
| [`iris version`](#iris-version) | Show version, build target, and JSON schema version |

## iris

```text
Iris generates and edits images (OpenAI GPT Image, Google Gemini "Nano Banana") and generates videos
(Google Veo) from one agent-friendly command line.

Every generation command names its model: pass -m/--model, or set the model in the config file
([image] model, [video] model). Iris never chooses a model for you; `iris models list` shows the
models it knows, what each is for, and the estimated cost of the same output from each (one
1024x1024 image or one 8-second 720p video).

Image commands are synchronous: the image is saved before the command returns. Video generation is a
provider-native job: Iris records it locally, waits, and saves the video; with --detach it returns a
job id that later commands (iris jobs status/wait/download) resume, even from another process that
uses the same state directory (`iris config path`).

Credentials are read only from the OPENAI_API_KEY and GEMINI_API_KEY environment variables. Provider
usage is billed by the provider to your API account; Iris reports cost estimates only.

With --json, every command prints exactly one JSON document on stdout (schema: `iris schema`);
progress and diagnostics go to stderr. Iris never prompts interactively.

Usage: iris [OPTIONS] <COMMAND>

Commands:
  image        Generate and edit images (synchronous; no job record)
  video        Generate videos (provider-native asynchronous jobs)
  jobs         List, inspect, wait for, download, and delete local video jobs
  models       List models and inspect their capabilities
  providers    List providers, credential variables, and whether they are set
  config       Show effective configuration and file locations
  doctor       Check credentials, configuration, and directories
  schema       Print the published JSON output schema
  completions  Print a shell completion script
  version      Show version, build target, and JSON schema version
  help         Print this message or the help of the given subcommand(s)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris image generate -m gpt-image-2.5-sunburst --quality low --size 1024x1024 "a fox" -o fox.png
  iris image edit -m nano-banana-2 -i photo.png "make it autumn" --json
  iris video generate -m veo-lite "waves at dusk" --duration 4 --detach
  iris jobs wait job_01jbz9k3m4n5p6q7r8s9t0v1w2
  iris models list --operation video.generate
  iris doctor

Exit codes: 0 success, 1 runtime or provider failure, 2 invalid or conflicting request: fix it
before retrying (error.provider_status null means nothing was sent; otherwise the provider rejected
it), 3 credentials/access/quota, 4 job not finished yet (it continues remotely), 5 outcome uncertain
(do not resubmit blindly), 130 interrupted (Ctrl-C/SIGINT, SIGTERM, or SIGHUP).

Provider usage is billed by the provider; `--dry-run` validates a request without sending it.
```

## iris image

```text
Generate images from a prompt, or edit/compose images from local reference images. Both are
synchronous paid requests: the images are validated and saved before the command returns, and no job
record is created.

Usage: iris image [OPTIONS] <COMMAND>

Commands:
  generate  Generate images from a prompt
  edit      Edit images, or compose a new image from reference images
  help      Print this message or the help of the given subcommand(s)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris image generate -m gpt-image-2.5-sunburst --quality low --size 1024x1024 "a fox" -o fox.png
  iris image edit -m nano-banana-2 -i photo.png "make it autumn" --json
```

## iris image generate

```text
Generate images from a text prompt. Synchronous: the paid request is sent once (never retried after
it may have been processed), and every returned image is validated and saved before the command
returns. Local validation (model, options, output paths, credentials) happens before anything is
sent.

Usage: iris image generate [OPTIONS] [PROMPT]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Prompt (exactly one source):
  -f, --prompt-file <PATH>
          Read the prompt from a UTF-8 file (trailing whitespace is trimmed)

      --prompt-stdin
          Read the prompt from standard input (must not be a terminal)

  [PROMPT]
          Prompt text

Model:
  -m, --model <MODEL>
          Model id or alias (see `iris models list`); required unless the config file sets
          image.model

      --capabilities-from <KNOWN_MODEL>
          Allow an unknown --model by declaring that it has this known model's capabilities

Generation options (validated against the model):
  -n, --count <N>
          Number of images

      --size <WxH|auto>
          Output size, WxH or auto

      --aspect-ratio <W:H>
          Aspect ratio, e.g. 16:9

      --resolution <R>
          Resolution class, e.g. 1K or 2K

      --quality <Q>
          Quality level, e.g. low, medium, high

      --format <FORMAT>
          Image format: png, jpeg, or webp

  -O, --option <KEY=VALUE>
          Provider-specific option KEY=VALUE (repeatable); see `iris models show <MODEL>`

Output:
  -o, --output <PATH>
          Exact output file; with several outputs: <stem>-<i>.<ext>, i from 1

  -d, --out-dir <DIR>
          Output directory (created if missing); default: IRIS_OUTPUT_DIR, config output_dir, or the
          current directory

      --overwrite
          Replace existing files (default: refuse with output_exists)

Execution:
      --dry-run
          Validate everything locally and print the plan; nothing is sent or charged

      --max-cost <USD>
          Spending cap in US dollars (e.g. 0.05): refuse to send the request if its estimated cost
          is higher, or if it has no estimate
          
          The cap compares the pre-call estimate, which can leave out prompt, input-image, and
          thinking tokens (its basis says what it leaves out), so the bill can be higher than the
          cap, by what the basis leaves out. A dry run refuses the same way, and its plan shows the
          cap on the cost line.

Examples:
  iris image generate -m gpt-image-2.5-sunburst --quality low --size 1024x1024 "a fox" -o fox.png
  iris image generate -m gpt-image-2 --quality low --size 1024x1024 "a red kite" --dry-run --json
  iris image generate -m gpt-image-2 --quality low --size 1024x1024 --max-cost 0.01 "a fox"
  iris image generate -m nano-banana-2 --aspect-ratio 16:9 -f prompt.txt -d out/
  echo "a lighthouse" | iris image generate -m nano-banana-2 --prompt-stdin --json

Provider usage is billed by the provider.
```

## iris image edit

```text
Edit one or more local images, or compose a new image from reference images, guided by a prompt.
Inputs are validated locally (format, size, count) before anything is sent. --mask is accepted only
by models that declare mask support.

Usage: iris image edit [OPTIONS] --image <PATH> [PROMPT]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Inputs:
  -i, --image <PATH>
          Input image (repeatable; at least one)

      --mask <PATH>
          Mask image (only for models that declare mask support)

Prompt (exactly one source):
  -f, --prompt-file <PATH>
          Read the prompt from a UTF-8 file (trailing whitespace is trimmed)

      --prompt-stdin
          Read the prompt from standard input (must not be a terminal)

  [PROMPT]
          Prompt text

Model:
  -m, --model <MODEL>
          Model id or alias (see `iris models list`); required unless the config file sets
          image.model

      --capabilities-from <KNOWN_MODEL>
          Allow an unknown --model by declaring that it has this known model's capabilities

Generation options (validated against the model):
  -n, --count <N>
          Number of images

      --size <WxH|auto>
          Output size, WxH or auto

      --aspect-ratio <W:H>
          Aspect ratio, e.g. 16:9

      --resolution <R>
          Resolution class, e.g. 1K or 2K

      --quality <Q>
          Quality level, e.g. low, medium, high

      --format <FORMAT>
          Image format: png, jpeg, or webp

  -O, --option <KEY=VALUE>
          Provider-specific option KEY=VALUE (repeatable); see `iris models show <MODEL>`

Output:
  -o, --output <PATH>
          Exact output file; with several outputs: <stem>-<i>.<ext>, i from 1

  -d, --out-dir <DIR>
          Output directory (created if missing); default: IRIS_OUTPUT_DIR, config output_dir, or the
          current directory

      --overwrite
          Replace existing files (default: refuse with output_exists)

Execution:
      --dry-run
          Validate everything locally and print the plan; nothing is sent or charged

      --max-cost <USD>
          Spending cap in US dollars (e.g. 0.05): refuse to send the request if its estimated cost
          is higher, or if it has no estimate
          
          The cap compares the pre-call estimate, which can leave out prompt, input-image, and
          thinking tokens (its basis says what it leaves out), so the bill can be higher than the
          cap, by what the basis leaves out. A dry run refuses the same way, and its plan shows the
          cap on the cost line.

Examples:
  iris image edit -m nano-banana-2 -i photo.png "make it autumn" -o fall.jpg
  iris image edit -m nano-banana-2 -i a.png -i b.png "combine these into one poster"
  iris image edit -m gpt-image-2.5-sunburst --quality low --size 1024x1024 -i room.png \
    --mask mask.png "add a window" --json

Provider usage is billed by the provider.
```

## iris video

```text
Generate videos as provider-native asynchronous jobs. Iris records each job locally before
submitting it, so it can be resumed by later commands.

Usage: iris video [OPTIONS] <COMMAND>

Commands:
  generate  Generate a video (waits and saves by default; --detach to submit and return)
  help      Print this message or the help of the given subcommand(s)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris video generate -m veo-lite "waves at dusk" --duration 4 -o waves.mp4
  iris video generate -m veo-lite "a paper boat" --detach --json
```

## iris video generate

```text
Generate a video as a provider-native asynchronous job. Iris writes a local job record before
submitting, then waits for the job and saves the video. With --detach it returns right after
submission; resume with `iris jobs wait <JOB_ID>` from any later process that uses the same state
directory (`iris config path` shows it): the job record lives there, so another machine, container,
or state directory cannot resume the job. The job also records where to save its outputs (-o, or the
output directory in effect, and --overwrite): a later `iris jobs wait` or `iris jobs download` given
neither -o nor -d saves there (result.job.output_plan).

If the caller's wait limit (--timeout) passes or you press Ctrl-C, the job keeps running remotely
and stays resumable (exit 4 or 130). If the outcome of the submission itself is uncertain, Iris
exits 5 and never resubmits automatically. If the command is killed before it prints the job id,
find the job with `iris jobs list --status submitting --json` (submission_unknown once the
submission budget has passed) by its model, created_at, and prompt_fingerprint, the prompt's SHA-256
and length, which --dry-run shows beforehand (result.prompt_fingerprint).

With --label, the command can be run again safely: no two local jobs share a label, so a second run
with the label is refused (label_in_use, naming the job) before anything is sent, and `iris jobs
list --label <LABEL>` finds the job.

Usage: iris video generate [OPTIONS] [PROMPT]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Inputs:
      --image <PATH>
          First frame image (image-to-video), if the model supports it

      --last-frame <PATH>
          Last frame image, if the model supports it

      --ref <PATH>
          Reference image (repeatable), if the model supports it

Prompt (exactly one source):
  -f, --prompt-file <PATH>
          Read the prompt from a UTF-8 file (trailing whitespace is trimmed)

      --prompt-stdin
          Read the prompt from standard input (must not be a terminal)

  [PROMPT]
          Prompt text

Model:
  -m, --model <MODEL>
          Model id or alias (see `iris models list`); required unless the config file sets
          video.model

      --capabilities-from <KNOWN_MODEL>
          Allow an unknown --model by declaring that it has this known model's capabilities

Generation options (validated against the model):
  -n, --count <N>
          Number of videos

      --duration <SECONDS>
          Video length in seconds

      --resolution <R>
          Resolution, e.g. 720p or 1080p

      --aspect-ratio <W:H>
          Aspect ratio, e.g. 16:9

      --negative-prompt <TEXT>
          What the video should not contain

  -O, --option <KEY=VALUE>
          Provider-specific option KEY=VALUE (repeatable); see `iris models show <MODEL>`

Output:
  -o, --output <PATH>
          Exact output file; with several outputs: <stem>-<i>.<ext>, i from 1

  -d, --out-dir <DIR>
          Output directory (created if missing); default: IRIS_OUTPUT_DIR, config output_dir, or the
          current directory

      --overwrite
          Replace existing files (default: refuse with output_exists)

Job:
      --label <LABEL>
          Label the job: 1-64 letters, digits, '.', '_', '-', starting with a letter or digit; no
          two local jobs share a label
          
          No other local job record may have the label, in any status. A script that gives the same
          label to the same intended video can therefore run again after a crash without paying
          twice: the second run is refused with label_in_use, which names the job and what to do for
          its status, before anything is sent. While a local job record cannot be read, a labeled
          submission is refused (state_invalid), since that record could have the label. Find the
          job with `iris jobs list --label <LABEL>`. The label is stored and shown as written (the
          prompt is kept only as a fingerprint), so it should not contain anything secret.

Waiting:
      --detach
          Submit, record the job, print its id, and return without waiting

      --timeout <DURATION>
          Caller wait limit (e.g. 90s, 10m, 1h, or seconds); the job continues remotely after it;
          default: IRIS_WAIT_TIMEOUT, config video.wait_timeout, or 10m

      --poll-interval <DURATION>
          Time between status checks (at least 2s); default: IRIS_POLL_INTERVAL, config
          video.poll_interval, or 10s

Execution:
      --dry-run
          Validate everything locally and print the plan; nothing is sent or charged

      --max-cost <USD>
          Spending cap in US dollars (e.g. 0.05): refuse to send the request if its estimated cost
          is higher, or if it has no estimate
          
          The cap compares the pre-call estimate, which can leave out prompt, input-image, and
          thinking tokens (its basis says what it leaves out), so the bill can be higher than the
          cap, by what the basis leaves out. A dry run refuses the same way, and its plan shows the
          cap on the cost line.

Examples:
  iris video generate -m veo-lite "waves at dusk" --duration 4 -o waves.mp4
  iris video generate -m veo-lite "a paper boat" --detach --json
  iris video generate -m veo-lite --image first.png "the scene comes alive" --timeout 15m
  iris video generate -m veo-lite "city timelapse" --dry-run --json
  iris video generate -m veo-lite "a paper boat" --duration 4 --max-cost 0.25 --detach
  iris video generate -m veo-lite "a paper boat" --duration 4 --label paper-boat-1 --detach --json

Provider usage is billed by the provider.
```

## iris jobs

```text
Work with local records of provider-native jobs (video generation). Records live in the state
directory (`iris config path`). Deleting a record never cancels or deletes anything remotely.

Usage: iris jobs [OPTIONS] <COMMAND>

Commands:
  list      List local job records (newest first)
  status    Show one job, refreshing its remote status once
  wait      Wait for a job to finish, then download its outputs
  download  Download the outputs of a finished job (never resubmits)
  delete    Delete LOCAL job records (no remote cancellation or deletion)
  help      Print this message or the help of the given subcommand(s)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris jobs list
  iris jobs wait job_01jbz9k3m4n5p6q7r8s9t0v1w2
  iris jobs download job_01jbz9k3m4n5p6q7r8s9t0v1w2 -o clip.mp4
```

## iris jobs list

```text
List local job records, newest first. Records that cannot be read are skipped with a
job_record_unreadable warning.

Each job shows its prompt's fingerprint (prompt_fingerprint: the SHA-256 of the prompt as sent, and
its length; never the text), to match a job to the request that created it: a record left submitting
by a process that was killed is found with --status submitting (submission_unknown once the
submission budget has passed), by model, created_at, and prompt_fingerprint.

Usage: iris jobs list [OPTIONS]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --status <STATUS>
          Only jobs with this status (submitting, submission_unknown, running, succeeded, failed,
          expired)

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

      --provider <PROVIDER>
          Only jobs of this provider

      --label <LABEL>
          Only the job with this label (`video generate --label`)

  -q, --quiet
          Suppress progress lines on stderr

      --limit <N>
          At most this many jobs

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris jobs list
  iris jobs list --status running --json
  iris jobs list --label paper-boat-1 --json
  iris jobs list --provider gemini --limit 5
```

## iris jobs status

```text
Show one job. A running job's remote status is checked once (a free status call) and recorded,
unless --no-refresh is given.

Exit status: 0 whenever the job's record could be read, whatever the job's state: branch on
result.job.status (submitting, submission_unknown, running, succeeded, failed, expired), not on the
exit code. A status check that fails is a status_refresh_failed warning, and the last known status
is shown. The command fails only when it cannot show the job, with that error's code (e.g. 2
job_not_found for an unknown id, 1 state_invalid for an unreadable record, 130 when interrupted).

Usage: iris jobs status [OPTIONS] <JOB_ID>

Arguments:
  <JOB_ID>
          Job id (job_ followed by 26 lowercase letters or digits)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --no-refresh
          Show the local record without checking the provider

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris jobs status job_01jbz9k3m4n5p6q7r8s9t0v1w2
  iris jobs status job_01jbz9k3m4n5p6q7r8s9t0v1w2 --no-refresh --json
```

## iris jobs wait

```text
Wait for a job to finish, then download its outputs (unless --no-download). Works from any process
that uses the same state directory: the job is resumed from its local record. The wait limit and
Ctrl-C only stop waiting; the job keeps running remotely. With --overwrite, outputs downloaded
earlier are fetched again and replace the saved files.

Outputs are saved to -o or -d when given; otherwise where video generate was asked to save them when
it submitted the job: its -o path, or the output directory in effect then, with its --overwrite
(result.job.output_plan).

Exit status: 0 once the job has succeeded and its outputs are saved (with --no-download, once it has
succeeded); 4 (wait_timeout) when the wait limit passes while the job continues remotely; 130 when
interrupted, the job continuing too. A job that ended without success exits with the code of its
recorded error, such as 1 for a remote failure, a blocked video, or an expired job
(remote_job_failed, content_blocked, artifact_expired), or 5 (submission_uncertain) when it is
unknown whether the provider accepted it. Other errors keep their own codes (e.g. 1 download_failed,
2 output_exists, 3 missing_credentials).

Usage: iris jobs wait [OPTIONS] <JOB_ID>

Arguments:
  <JOB_ID>
          Job id

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --timeout <DURATION>
          Caller wait limit (e.g. 90s, 10m, 1h, or seconds); the job continues remotely after it;
          default: IRIS_WAIT_TIMEOUT, config video.wait_timeout, or 10m

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

      --poll-interval <DURATION>
          Time between status checks (at least 2s); default: IRIS_POLL_INTERVAL, config
          video.poll_interval, or 10s

      --no-download
          Only wait; do not download the outputs

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Output:
  -o, --output <PATH>
          Exact output file; with several outputs: <stem>-<i>.<ext>, i from 1. Without -o or -d:
          where the job was submitted to save (result.job.output_plan)

  -d, --out-dir <DIR>
          Output directory (created if missing). Without -o or -d: where the job was submitted to
          save (result.job.output_plan)

      --overwrite
          Replace existing files (default: refuse with output_exists)

Examples:
  iris jobs wait job_01jbz9k3m4n5p6q7r8s9t0v1w2
  iris jobs wait job_01jbz9k3m4n5p6q7r8s9t0v1w2 --timeout 30m -d videos/ --json
  iris jobs wait job_01jbz9k3m4n5p6q7r8s9t0v1w2 --no-download
```

## iris jobs download

```text
Download the outputs of a succeeded job. Nothing is ever regenerated or resubmitted. Repeating a
download is safe: an intact file already at the target is reported as already_downloaded, and an
intact earlier download is copied locally. With --overwrite, every output is fetched from the
provider again and replaces the saved file atomically.

Outputs are saved to -o or -d when given; otherwise where video generate was asked to save them when
it submitted the job: its -o path, or the output directory in effect then, with its --overwrite
(result.job.output_plan).

Exit status: 0 once the job's outputs are saved. A job whose record still reads running is checked
once first (a free status call): 4 (job_not_ready) while it is still running, or when the check
failed for a transient reason; a check that fails for any other reason exits with that error's code
(e.g. 3 missing_credentials). A job that ended without success exits with the code of its recorded
error, such as 1 (remote_job_failed, content_blocked, artifact_expired) or 5 (submission_uncertain).
Other errors keep their own codes (e.g. 1 download_failed, 2 output_exists, 130 interrupted).

Usage: iris jobs download [OPTIONS] <JOB_ID>

Arguments:
  <JOB_ID>
          Job id

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Output:
  -o, --output <PATH>
          Exact output file; with several outputs: <stem>-<i>.<ext>, i from 1. Without -o or -d:
          where the job was submitted to save (result.job.output_plan)

  -d, --out-dir <DIR>
          Output directory (created if missing). Without -o or -d: where the job was submitted to
          save (result.job.output_plan)

      --overwrite
          Replace existing files (default: refuse with output_exists)

Examples:
  iris jobs download job_01jbz9k3m4n5p6q7r8s9t0v1w2
  iris jobs download job_01jbz9k3m4n5p6q7r8s9t0v1w2 -o clip.mp4 --json
```

## iris jobs delete

```text
Delete local job records only. Remote jobs are not cancelled and downloaded files are not deleted.
Deletion is all or nothing. Without --force, jobs that are still submitting or running, and
succeeded jobs whose outputs were not downloaded while the provider still keeps them, are refused
(they would become unrecoverable). With --all --force, unreadable job record files are deleted too.

Usage: iris jobs delete [OPTIONS] [JOB_ID]...

Arguments:
  [JOB_ID]...
          Job ids to delete

Options:
      --all
          Delete every local job record

      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

      --force
          Also delete jobs that are still submitting or running, or whose outputs were not
          downloaded (with --all: also unreadable job records)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris jobs delete job_01jbz9k3m4n5p6q7r8s9t0v1w2
  iris jobs delete --all
  iris jobs delete --all --force --json
```

## iris models

```text
List the models Iris knows, with what each is for and its estimated cost for the same output, and
inspect their declared capabilities, options (with their defaults), and published prices.

Usage: iris models [OPTIONS] <COMMAND>

Commands:
  list  List known models
  show  Show one model's capabilities, options, and prices
  help  Print this message or the help of the given subcommand(s)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris models list
  iris models show nano-banana-2 --json
```

## iris models list

```text
List the models in Iris's catalog with their provider, lifecycle, operations, and aliases, a
one-line summary of what each is for, and its estimated cost for a standard output, the same for
every model of an operation kind so that costs compare: one 1024x1024 image (at each quality for the
OpenAI models) or one 8-second 720p video. It reads only the catalog, so it runs even when the
config file is invalid.

Usage: iris models list [OPTIONS]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --provider <PROVIDER>
          Only models of this provider

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

      --operation <OPERATION>
          Only models supporting this operation (image.generate, image.edit, video.generate)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris models list
  iris models list --provider gemini --json
  iris models list --operation image.edit
```

## iris models show

```text
Show one model's summary, billing, and declared capabilities: operations, inputs, options (with the
typed flag or -O key and the default of each), output types, limits, published prices, the estimated
cost of the standard output that `models list` compares models on, with the options that give it,
and documented access requirements. Without --check-access it reads only the catalog and whether the
API key is set, so it runs even when the config file is invalid. --check-access also asks the
provider with a free metadata call whether the model is visible to your key; billing tier, prepaid
credit, and organization verification are not checked.

Usage: iris models show [OPTIONS] <MODEL>

Arguments:
  <MODEL>
          Model id or alias

Options:
      --check-access
          Check with a free metadata call whether the model is visible to your key (needs the
          provider's API key)

      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris models show nano-banana-2
  iris models show gpt-image-2 --json
  iris models show veo-fast --check-access
```

## iris providers

```text
List providers with the environment variable each reads its API key from and whether it is set
(never its value).

Usage: iris providers [OPTIONS] <COMMAND>

Commands:
  list  List providers, credential variables, presence, and base URLs
  help  Print this message or the help of the given subcommand(s)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris providers list
  iris providers list --json
```

## iris providers list

```text
List every provider Iris supports with its operations, the environment variable its API key is read
from (OPENAI_API_KEY or GEMINI_API_KEY), whether that variable is set (never its value), and the
base URL requests go to. The key is sent to that base URL, including an override from
IRIS_OPENAI_BASE_URL, IRIS_GEMINI_BASE_URL, or the config file.

Usage: iris providers list [OPTIONS]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris providers list
  iris providers list --json
```

## iris config

```text
Show the effective non-secret settings with the source of each value (flag, env, file, default), and
where Iris keeps its files.

Usage: iris config [OPTIONS] <COMMAND>

Commands:
  show  Show effective settings and the source of each value
  path  Show the config file, state directory, and jobs directory paths
  help  Print this message or the help of the given subcommand(s)

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris config show
  iris config path --json
```

## iris config show

```text
Show every non-secret setting Iris would use, with its value and where it came from: a command-line
flag, an environment variable (named), the config file, or the built-in default. Credentials are
listed by presence only, never by value. A non-default provider base URL is flagged with a warning,
since API keys are sent there.

Usage: iris config show [OPTIONS]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris config show
  iris --config ./iris.toml config show --json
```

## iris config path

```text
Show the absolute paths Iris uses: the config file (--config, IRIS_CONFIG, or the platform default;
it need not exist), the state directory (IRIS_STATE_DIR, config state_dir, or the platform default),
and the jobs directory inside it where local job records are kept.

Usage: iris config path [OPTIONS]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris config path
  iris config path --json
```

## iris doctor

```text
Check credential presence (never values; no provider key at all is an error, since every generation
command would fail), configuration validity, state and output directory writability, and base URL
overrides. --check-access additionally makes free metadata calls to see whether every catalog model
of each provider whose API key is set is visible to your key; they do not check billing tier,
prepaid credit, or organization verification, so a paid request can still be refused.

Exit status: doctor exits 0 whenever its checks ran, even when it finds problems. Read `healthy`
(result.healthy with --json) or look for [error] lines instead of relying on the exit code.

Usage: iris doctor [OPTIONS]

Options:
      --check-access
          Also check with free metadata calls whether each model of a provider whose API key is set
          is visible to your key

      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris doctor
  iris doctor --check-access --json
```

## iris schema

```text
Print the versioned JSON Schema of the --json output envelope, including every result type in $defs.
Without --json the raw schema document is printed.

Usage: iris schema [OPTIONS]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris schema > iris-output.v1.schema.json
  iris schema --json
```

## iris completions

```text
Print a completion script for bash, zsh, fish, or elvish on stdout.

Usage: iris completions [OPTIONS] <SHELL>

Arguments:
  <SHELL>
          Shell to generate completions for
          
          [possible values: bash, zsh, fish, elvish]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris completions bash > ~/.local/share/bash-completion/completions/iris
  iris completions zsh > "${fpath[1]}/_iris"
  iris completions fish > ~/.config/fish/completions/iris.fish
```

## iris version

```text
Show the Iris version, the target it was built for, and the JSON output schema version.

Usage: iris version [OPTIONS]

Options:
      --json
          Machine mode: exactly one JSON document on stdout; progress and diagnostics on stderr

      --config <PATH>
          Config file (overrides IRIS_CONFIG and the platform default)

  -q, --quiet
          Suppress progress lines on stderr

  -v, --verbose...
          More diagnostics on stderr (repeatable); never prints prompts, keys, or signed URLs

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

Examples:
  iris version
  iris version --json
```
