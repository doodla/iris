THIRD-PARTY SOFTWARE NOTICES AND LICENSES

The iris executable in this archive contains code from the open-source Rust
crates listed below, each built from its unmodified release on crates.io.
Iris itself is licensed under the MIT license in LICENSE. Each crate is used
under the license it is listed with here; the full text of every license
follows, with each crate's own copyright notices where the crate ships them.

Source code: the source of every crate below, exactly as used, is available
from crates.io at the address listed with it. This is also where to get the
source code of the crates used under the Mozilla Public License 2.0 (MPL-2.0).

This file was generated by cargo-about (https://github.com/EmbarkStudios/cargo-about)
from Iris's Cargo.lock, for the target this archive was built for.

Licenses:
{{#each overview}}
  {{{name}}} ({{{id}}})
{{/each}}
{{#each licenses}}

================================================================================
{{{name}}} ({{{id}}})
================================================================================

Used by:
{{#each used_by}}
{{#if crate.source}}
  {{{crate.name}}} {{{crate.version}}}: https://crates.io/crates/{{{crate.name}}}/{{{crate.version}}}
{{else}}
  {{{crate.name}}} {{{crate.version}}}: this program itself
{{/if}}
{{/each}}
{{#if (eq id "MPL-2.0")}}

The crates above are used unmodified. Their source code, the Source Code Form
under the MPL-2.0, is available at the crates.io addresses above.
{{/if}}

{{{text}}}
{{/each}}
